version: '3.8'

services:
  flooring-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: upstairs-flooring-app
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
